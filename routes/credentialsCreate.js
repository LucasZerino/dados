const express = require("express");
const axios = require("axios");

const router = express.Router();

router.post("/create/sga/pensar", async (req, res) => {});
router.post("/create/sga/desativar", async (req, res) => {});
